# Privox Runtime Wheels

This directory contains pre-built Python wheels used for the automated environment setup on Windows.
- `llama_cpp_python`: Specifically built with CUDA support to ensure GPU acceleration works out-of-the-box.

_Release v1.1_
